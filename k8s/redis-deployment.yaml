apiVersion: apps/v1
kind: Deployment
metadata:
  name: todo-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: todo-redis
  template:
    metadata:
      labels:
        app: todo-redis
    spec:
      containers:
        - name: redis
          image: redis:7
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: todo-redis
spec:
  ports:
    - port: 6379
  selector:
    app: todo-redis
